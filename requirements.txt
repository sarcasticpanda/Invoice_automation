# Backend dependencies (pinned to the working local environment).
# Complete set required to actually start app_server.py + run the pipeline.

# --- Web server ---
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27        # required for file uploads (UploadFile)
gunicorn

# --- LangChain / LangGraph ---
langchain==1.2.17
langchain-core==1.3.3
langchain-community==0.4.1
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-google-genai==4.2.2
langchain-groq==1.1.2
langgraph==1.1.10

# --- Vector store + embeddings ---
chromadb==1.5.9
sentence-transformers==5.4.1    # heavy: pulls torch. See DEPLOY.md RAM note.
torch==2.11.0

# --- Document loaders ---
pypdf==6.10.2
python-docx==1.2.0
beautifulsoup4==4.14.3

# --- Gmail API (email pipeline) ---
google-api-python-client==2.195.0
google-auth==2.50.0
google-auth-oauthlib==1.3.1
google-auth-httplib2==0.3.1

# --- Misc ---
httpx==0.28.1                   # Supabase REST (chat history)
python-dotenv==1.2.2
colorama==0.4.6
pydantic==2.13.3
